# Discord Bot Settings / Discord 機器人設定
# Note: Please replace the following with the actual token and ID obtained from the Discord Developer Portal
# 注意：請將以下替換為從 Discord Developer Portal 獲取的實際令牌和ID
DISCORD_BOT_TOKEN=your_bot_token_here
DISCORD_CLIENT_ID=your_application_id_here
# Server ID (optional, leave empty to deploy commands globally)
# 伺服器 ID（可選，留空將全局部署命令）
DISCORD_GUILD_ID=
# Discord Developer Portal client secret (for web interface)
# Discord開發者門戶的客戶端密鑰（Web介面使用）
DISCORD_CLIENT_SECRET=

# Web Interface Settings (optional) / Web界面設定（可選）
ENABLE_WEB_INTERFACE=false
WEB_PORT=3000
WEB_HOST=0.0.0.0
SESSION_SECRET=random_string_for_session_encryption
DISCORD_CALLBACK_URL=http://localhost:3000/auth/discord/callback

# Translation Service Settings (at least one is required)
# 翻譯服務設定（至少需要一個）
# DeepL API (recommended, has free tier)
# DeepL API (推薦，有免費額度)
DEEPL_API_KEY=your_deepl_api_key
# Google API (paid service)
# Google API（付費服務）
GOOGLE_API_KEY=your_google_api_key

# Bot Settings / 機器人設定
NODE_ENV=development
LOG_LEVEL=info
DEFAULT_SOURCE_LANG=zh-TW
DEFAULT_TARGET_LANG=en-US